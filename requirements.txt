openai>=1.12.0
anthropic>=0.18.0
pydantic>=2.0.0
jinja2>=3.1.0
python-dotenv>=1.0.0

# Manga generation (install manga-ai-bot and bubble as editable packages):
#   pip install -e /path/to/manga-ai-bot
#   pip install -e /path/to/bubble
# Their dependencies (torch, diffusers, pillow, etc.) are pulled in automatically.
